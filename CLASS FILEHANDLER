#include <iostream>
#include <fstream>
using namespace std;

class FileHandler
{
private:
    ofstream file;

public:
    // Constructor
    FileHandler()
    {
        file.open("sample.txt");

        if (file.is_open())
        {
            cout << "File opened successfully." << endl;
            file << "This is a sample file." << endl;
        }
        else
        {
            cout << "Unable to open file." << endl;
        }
    }

    // Destructor
    ~FileHandler()
    {
        if (file.is_open())
        {
            file.close();
            cout << "File closed successfully." << endl;
        }
    }
};

int main()
{
    FileHandler f1;

    cout << "Working with the file..." << endl;

    return 0;
}
